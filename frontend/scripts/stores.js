import { writable } from 'svelte/store';

export const title = writable("<позиция>");
export const formatTime = writable("<формат>");
export const formatOffice = writable("<формат>");
export const location = writable("<локация>");
export const salary = writable("<вилка>");
export const description = writable("<описание>");
